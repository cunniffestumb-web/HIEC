# HIEC 全息智能教育云技术优化报告

## 一、交付结果

本次没有覆盖原项目，而是在同级目录新增了 `HIEC-技术优化版`。原项目、宣传册、PPT、PDF 和材料源文件仍然保留。本目录包含一份可以单独启动的技术优化版本。

本次工作的重点是技术稳定性和可运行性，不改动项目原有的宣传叙事，也没有删除 8K、120Hz、低延迟、市场规划等愿景内容。它们仍可用于比赛材料，但技术上应理解为目标指标或发展规划。

## 二、项目全景：项目在做什么

HIEC 是一个面向沉浸式教育场景的 AI 教育产品原型。项目希望把教师的教学流程交给一组教育智能体协同完成，而不是只提供一个普通聊天框。

核心流程是：

```text
教学目标输入
  -> 学情诊断
  -> 个性化推荐
  -> 内容生成
  -> 3D 场景展示
  -> AI 伴学答疑
  -> 练习与评估
  -> 教学结果审查
```

项目中的 Edu-MAS 可以理解为 AI 教学团队。诊断、推荐、内容、伴学、评估和审查分别承担不同职责。五层技术架构负责承载这些能力，网页、3D 和未来的 AR/VR 终端负责呈现学习体验。

## 三、原项目当前实际状态

原网站由 24 个独立 HTML 页面、CSS、JavaScript 和 Three.js 3D 脚本组成，没有完整的数据库、登录系统、权限系统或生产级后端。

已经具备的内容：

- 首页、功能、场景、架构、路线图、团队、资源等产品展示页面；
- 解剖、化学、历史、天文四类教学场景；
- 化学分子和天文系统的浏览器 3D Demo；
- 基于规则的本地评估和雷达图；
- AI 学习助手的前端交互界面；
- 白板、聊天、语音、屏幕共享和 3D 协作等交互界面。

仍属于原型或模拟的内容：

- Edu-MAS 多智能体目前主要体现在架构和交互设计中，尚不是完整的多智能体编排服务；
- 多数多人协作页面原来通过定时器和随机数模拟用户行为；
- WebRTC、WebSocket、Redis、数据库、登录和权限没有形成完整生产服务；
- 文件上传原来主要用于显示附件，并没有完整的文档解析和多模态理解链路；
- 3D 分子是教学展示模型，不是完整化学计算引擎；
- 8K、20ms、99.9%、学习效率提升等指标没有随代码提供测试报告。

## 四、本次完成的技术优化

### 1. 新增全栈服务层

新增 `应用/server.js`，使用 Node.js 原生 HTTP 服务，统一处理：

- 静态页面和资源；
- `/api/health` 健康检查；
- `/api/config` 前端服务配置；
- `/api/chat` AI 对话接口；
- `/ws` WebSocket 协作房间。

项目不再只有静态页面，可以通过一个命令启动前端和后端。

### 2. AI 接口支持浏览器直连和服务端代理

原代码把真实 DeepSeek API Key 硬编码在浏览器端。优化版已经删除源码中的真实密钥，设置页恢复了 DeepSeek 的直接调用能力：用户可以填写 API 地址、模型和自己的 Key，浏览器直接向模型服务发送请求；Key 仅保存在当前浏览器的本地存储中，不进入项目源码。

同时保留 `/api/chat` 服务端代理和离线演示模式。默认页面可以切换到本地服务/离线模式，输入“甲烷键角”“天文轨道”“人体器官”等关键词时返回示例教学回答；需要正式部署时仍推荐使用服务端代理，不让共享网址的所有访问者接触同一个浏览器 Key。

### 3. 增加真实的轻量 WebSocket 协作

新增 `scripts/realtime-collaboration.js` 和 `ws` 服务依赖。

当前 3D 协作页可以在同一房间中同步：

- 讨论消息；
- 分子旋转、缩放等操作提示；
- 在线人数。

如果 WebSocket 服务不可用，原有的本地模拟效果仍然保留，因此不影响离线展示。当前服务是比赛 Demo 级别，尚未加入账号、权限、历史持久化和多人冲突解决。

### 4. 修复用户输入安全问题

聊天、评论、附件名称、协作标注和活动记录原来有多处直接拼接到 `innerHTML` 的写法。优化版改用 `textContent` 和 DOM 节点构建，减少用户输入被当作 HTML 执行的风险。

同时限制聊天附件大小为 2 MB，附件只把名称和类型摘要提交给服务端，不把本地文件内容无条件发送出去。

### 5. 修复 3D 协作页面结构

清除了 `3d-collaboration.html` 结束 `</html>` 后残留的代码，解决了重复 `chatMessages`、重复 `sendMessage` 和重复浮动聊天初始化造成的函数覆盖问题。

页面现在保留一个主聊天面板，并把实时协作逻辑单独放入脚本文件。

### 6. 增强离线运行能力

- Three.js 统一使用 0.160.0，并放入 `vendor/three/three.min.js`；
- Font Awesome 放入 `vendor/fontawesome/`；
- 天文、化学和 3D 协作页面不再依赖 Three.js CDN；
- Service Worker 增加四个场景、3D 脚本、字体图标和项目文档缓存；
- `script.js` 在 HTTP(S) 环境自动注册 Service Worker；
- 修复天文页多余的 `</script>`。

Google Fonts 仍作为可选外部字体存在。即使字体加载失败，页面也会使用系统字体继续显示。

### 7. 修复页面路由和文档入口

- 修正首页功能卡片与 `features.html` 实际模块 ID 不一致的问题；
- 删除 Vercel 把所有页面重写到 `index.html` 的配置；
- 新增真实的架构、API 和部署说明文件；
- 架构页的下载和 API 文档入口不再只是打印日志或显示假完成提示。

## 五、优化版目录说明

```text
HIEC-技术优化版/
|-- 应用/
|   |-- index.html
|   |-- anatomy.html
|   |-- chemistry.html
|   |-- history.html
|   |-- astronomy.html
|   |-- 3d-collaboration.html
|   |-- server.js
|   |-- package.json
|   |-- package-lock.json
|   |-- .env.example
|   |-- docs/
|   |-- scripts/
|   |-- vendor/
|   `-- 其余原有页面和资源
|-- 材料-原版/
`-- 技术优化报告.md
```

## 六、验证结果

在优化版目录执行并通过：

- `npm run check`：服务端、主脚本、天文脚本、化学脚本语法检查通过；
- `node --check scripts/realtime-collaboration.js`：协作脚本语法通过；
- `npm audit --omit=dev`：0 个漏洞；
- 24 个 HTML 页面 HTTP 状态均为 200；
- `/api/health` 返回服务运行状态；
- `/api/config` 返回离线模式；
- `/api/chat` 可以返回离线化学教学回答；
- 两个 WebSocket 客户端可以在同一房间互相收到聊天消息；
- 本地 Three.js、Font Awesome 和四个场景资源均可正常访问；
- HTML 结构扫描未发现多个 `doctype`、`body` 或 `html` 结束标签的问题。

## 七、如何启动和演示

进入：

```text
D:\比赛\易班\AI教育\HIEC-技术优化版\应用
```

启动：

```bash
npm install
npm start
```

浏览器访问：

```text
http://localhost:5173/
```

推荐演示顺序：

1. 首页展示项目整体定位；
2. 进入功能页展示 AI 学习助手和智能评估；
3. 进入化学场景展示 3D 分子旋转、缩放和教学观察；
4. 进入 3D 协作页展示聊天、在线人数和协作操作广播；
5. 返回场景页，再展示解剖、历史和天文三个场景，说明平台能够承载多学科内容。

## 八、下一步建议

下一步仍以技术为主，建议按以下顺序推进：

1. 为 3D 协作补充身份、房间、权限和操作记录；
2. 为评估模块增加固定题库、答题记录和导出报告；
3. 为 AI 服务增加请求日志、超时、重试、限流和模型调用审计；
4. 把四个场景都接入统一的“教学目标、诊断、推荐、伴学、评估”演示协议；
5. 增加浏览器自动化测试，至少覆盖首页、四个场景、AI 对话和协作页面；
6. 后续再考虑把静态多页面整理成组件化前端，不建议现在直接重写成大型微服务系统。

## 九、比赛材料口径说明

本次优化没有删除原有宣传大饼。建议在答辩中使用这样的表达：

> 当前版本完成了教育多智能体架构设计、四类沉浸式教学场景和交互原型验证；真实大规模部署、硬件融合、性能目标和商业化指标属于后续规划。

这样既保留项目的想象空间，也能准确回答评委对当前代码和 Demo 的追问。

## 十、网站访问说明

### 10.1 为什么别人之前打不开

`localhost` 只指向访问者自己的电脑，因此把 `http://localhost:5173/` 发给别人没有用。局域网地址只能在同一 Wi-Fi 或同一内网使用，手机和其他电脑还可能受到 Windows 防火墙、路由器隔离或网络切换影响。真正让所有人通过互联网打开，需要公网服务器或公网隧道。

### 10.2 当前可用入口

本轮已启动本机全栈服务，并通过临时公网隧道验证首页和 `/api/health` 可以从互联网访问。临时地址依赖本机服务和隧道进程，关闭进程后失效，也可能因免费隧道重连而改变，不能作为永久网址。当前这类地址只适合临时发给评委或队友检查。

本机访问使用 `http://localhost:5173/`；同一局域网访问使用服务器启动日志打印的 `http://局域网IP:5173/`。需要长期公开访问时，推荐使用根目录的 `render.yaml` 部署到 Render；也可以登录 Vercel 部署静态页面和 `api/` 接口。Vercel 不支持本项目的 WebSocket 长连接，页面已加入能力探测，会自动使用本地协作演示，不再持续报连接错误。

### 10.3 本轮公网适配

- 新增 `应用/api/health.js`、`应用/api/config.js` 和 `应用/api/chat.js`，使 Vercel 等函数托管可以提供同源健康检查、配置和离线/在线 AI 接口。
- 新增 `应用/api/_lib/chat.js`，复用输入限制、离线知识库和服务端 DeepSeek 代理逻辑。
- `scripts/realtime-collaboration.js` 会先读取健康接口，托管平台不支持 WebSocket 时不再反复重连。
- `script.js` 增加静态托管下的离线回答兜底，AI 接口暂时不可用时仍能完成演示。
- 新增 `render.yaml`，可直接按 Node Web Service 部署完整服务，包括 WebSocket。

## 十一、最新验收结论

截至 2026-09-01，优化版已完成：24 个 HTML 页面桌面端和移动端检查、静态资源返回检查、天文和化学 3D Canvas 检查、Service Worker 激活检查、AI 离线接口检查、WebSocket 本地房间检查、服务端和前端脚本语法检查，以及生产依赖漏洞检查。当前 `npm run check` 通过，`npm audit --omit=dev` 为 0 个漏洞。

需要区分两种状态：本地启动的 `server.js` 是完整演示服务，支持 WebSocket；Vercel 等函数托管是公开网页部署方案，支持 AI 接口但会让协作页降级。两者都能打开网页，但能力边界不同。
